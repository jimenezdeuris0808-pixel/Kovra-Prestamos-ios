/// Escala de radios del sistema de diseño clay de Kovra.
///
/// Ver `DESIGN_SYSTEM_CLAY.md`, sección "2. Escala de radios".
class AppRadii {
  AppRadii._();

  /// Chips pequeños, badges compactos, checkboxes.
  static const double xs = 8;

  /// Inputs de formulario, botones estándar.
  static const double sm = 12;

  /// Tarjetas de contenido: cliente_card, cuota_card, tiles de lista, etc.
  static const double md = 16;

  /// Tarjetas destacadas, modales, bottom sheets, tarjeta de login.
  static const double lg = 20;

  /// Headers hero con gradiente.
  static const double xl = 28;

  /// Tabs tipo pill, badges de estado, avatares, FAB.
  static const double pill = 999.0;
}
